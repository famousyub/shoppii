import { SearchFilterprodPipe } from './search-filterprod.pipe';

describe('SearchFilterprodPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterprodPipe();
    expect(pipe).toBeTruthy();
  });
});
